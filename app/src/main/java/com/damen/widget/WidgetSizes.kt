package com.damen.widget

import androidx.compose.ui.unit.DpSize
import androidx.compose.ui.unit.dp

// Responsive kompozisyon boyutları (Glance en uygun olanı seçer,
// içerik LocalSize ile ayrıca uyarlanır).
val BoxS = DpSize(110.dp, 110.dp)
val BoxM = DpSize(160.dp, 160.dp)
val BoxL = DpSize(220.dp, 220.dp)

val BarS = DpSize(220.dp, 100.dp)
val BarM = DpSize(360.dp, 110.dp)
val BarL = DpSize(500.dp, 110.dp)

val PanelS = DpSize(300.dp, 160.dp)
val PanelM = DpSize(420.dp, 200.dp)
val PanelL = DpSize(560.dp, 260.dp)
